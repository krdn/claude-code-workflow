# Welcome 메시지
# 첫 기여자에게 환영 메시지를 보냅니다.

name: Welcome

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome:
    name: Welcome New Contributors
    runs-on: ubuntu-latest

    steps:
      - name: Welcome First-time Issue Creator
        if: github.event_name == 'issues'
        uses: actions/first-interaction@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 안녕하세요 @${{ github.actor }}님!

            **Claude Code Workflow** 프로젝트에 첫 이슈를 등록해 주셔서 감사합니다! 🎉

            이슈를 검토하고 가능한 빨리 답변드리겠습니다.

            그동안 다음 문서들이 도움이 될 수 있습니다:
            - 📚 [Wiki 문서](https://github.com/krdn/claude-code-workflow/wiki)
            - 💬 [GitHub Discussions](https://github.com/krdn/claude-code-workflow/discussions)

            감사합니다! 🙏

      - name: Welcome First-time PR Creator
        if: github.event_name == 'pull_request_target'
        uses: actions/first-interaction@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          pr-message: |
            👋 안녕하세요 @${{ github.actor }}님!

            **Claude Code Workflow** 프로젝트에 첫 PR을 제출해 주셔서 감사합니다! 🎉

            PR을 검토하고 피드백을 드리겠습니다. 잠시만 기다려 주세요.

            **체크리스트**:
            - [ ] 커밋 메시지가 [컨벤션](https://github.com/krdn/claude-code-workflow/blob/main/CONTRIBUTING.md#커밋-컨벤션)을 따르나요?
            - [ ] 관련 Issue가 있다면 연결했나요?

            기여해 주셔서 감사합니다! 🙏
